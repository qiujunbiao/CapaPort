{
  "name": "@agentdoor/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "agentdoor": "dist/agentdoor.mjs"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && tsx scripts/build.ts",
    "test": "vitest run",
    "test:e2e": "pnpm --filter @agentdoor/adapter-sdk build && vitest run --config vitest.e2e.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agentdoor/adapter-claude-code": "workspace:*",
    "@agentdoor/adapter-codex": "workspace:*",
    "@agentdoor/adapter-cursor": "workspace:*",
    "@agentdoor/adapter-gemini-cli": "workspace:*",
    "@agentdoor/adapter-sdk": "workspace:*",
    "@agentdoor/capability-kit": "workspace:*",
    "@agentdoor/contracts": "workspace:*",
    "@agentdoor/security-scan": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "26.1.2",
    "esbuild": "0.28.1",
    "tsx": "4.23.10",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
