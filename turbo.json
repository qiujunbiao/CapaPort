{
  "$schema": "https://turbo.build/schema.json",
  "globalPassThroughEnv": [
    "CAPAPORT_ACCEPTANCE_STAMP",
    "CAPAPORT_API_URL",
    "CAPAPORT_MAILPIT_URL",
    "CAPAPORT_REGISTRY",
    "npm_package_version"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "outputs": []
    }
  }
}
