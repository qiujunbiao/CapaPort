# Agentdoor 产品需求文档

版本：1.0

日期：2026-08-07

状态：架构评审稿

## 1. 概要

Agentdoor 是跨 Agent 工具的企业级 AI 能力分发与治理平台。它通过桌面客户端、云端服务、Web 管理后台和 CLI，把员工在 Codex、Claude Code、Cursor、Gemini CLI 等工具中形成的 Skill、Prompt 和项目上下文沉淀为可审核、可安装、可更新、可追踪的组织资产。

MVP 首先服务研发团队和 AI 高阶用户，验证“发现 → 沉淀 → 审核 → 安装 → 更新”闭环。员工继续使用熟悉的 Agent 工具，Agentdoor 负责组织资产的标准化、共享、权限、安全和持续迭代。

## 2. 联系人与职责

| 角色 | 负责人 | 职责 |
| --- | --- | --- |
| 产品负责人 | 项目发起人 | 产品范围、优先级和最终验收 |
| 产品与架构执行 | Agentdoor 项目组 | PRD、交互、技术架构、研发和质量 |
| 组织管理员代表 | 首批试点团队管理员 | 账号、组织、审核和治理流程验收 |
| 员工用户代表 | 首批研发团队与 AI 高阶用户 | 本地发现、发布、安装和更新体验验收 |

## 3. 背景

企业员工会长期并行使用多种 Agent 工具。工具之间缺少统一的能力格式、资产目录和协作方式，造成三个核心问题：

1. 工具分散：不同员工和场景使用不同 Agent，能力难以跨工具分发。
2. 信息分散：AI 工作成果留在个人电脑，通过文件或代码地址低效传播。
3. 重复建设：员工不知道组织中已有能力，持续重复制作相似 Skill、Prompt 和上下文。

Agentdoor 不替代 Agent。它站在 Agent 之上，建立平台中立的能力包规范与组织治理层。个人或企业使用的 Agent 可以变化，但组织积累的知识、上下文和能力不应被单一工具锁定。

参考 Skill Zoo 的本地扫描、安装、更新、归档和安全确认思路，但 Agentdoor 不直接复制其个人工具产品模型。Agentdoor 从第一天就把账号、多租户组织、空间权限、审核、审计和云端分发作为主干。

## 4. 目标

### 4.1 产品目标

- 让员工在不改变主要 Agent 工作习惯的前提下沉淀个人能力。
- 让组织建立统一、可搜索、可治理的 AI 能力目录。
- 让能力在多个 Agent 工具之间安全安装和持续更新。
- 让组织知道能力由谁发布、谁审核、谁安装、使用哪个版本。
- 让项目上下文只同步被明确选择的规则与知识，不上传业务源码。

### 4.2 MVP 关键结果

- 试点成员中至少 60% 在首个试点周期内成功发现或创建一个能力包。
- 至少 30% 的有效能力包完成一次从个人空间到团队、项目或组织空间的发布。
- 已发布能力包至少产生一次来自其他成员的安装，复用率达到 40%。
- 90% 的受支持 Agent 安装任务一次成功；失败任务提供可操作的错误信息和恢复方式。
- 已安装能力的新版本发布后，90% 的在线客户端在一个同步周期内提示更新。
- 组织级发布、审核、下载、安装和删除操作的审计覆盖率为 100%。
- 租户隔离、安全扫描和权限越权测试不得存在高危未关闭问题。

### 4.3 非目标

MVP 不构建通用 Agent 运行平台，不托管模型推理，不上传或索引业务源码，不支持外部访客与跨组织共享，不做复杂计费，不提供语义知识问答，也不为每种 Agent 单独开发插件。

## 5. 市场与用户

### 5.1 首批用户

首批用户是已经使用 Codex、Claude Code、Cursor 或 Gemini CLI 的研发团队和 AI 高阶用户。他们具备本地文件、项目目录和版本概念，但不应被要求理解 Agentdoor 的内部存储格式。

### 5.2 核心角色

- 员工用户：发现、编辑、安装和更新能力包。
- 能力贡献者：把个人能力提交到团队、项目或组织空间。
- 审核者：检查内容质量、安全风险、适用范围和版本变化。
- 空间负责人：维护团队或项目成员、审核规则和空间状态。
- 组织管理员：维护组织成员、组织政策、组织级审核与治理。
- 安全审计员：只读查看风险报告和审计事件。

### 5.3 主要任务

- 当我在本地已经有好用的 Skill 或 Prompt 时，我希望快速发现并保存，避免重新录入。
- 当团队遇到相同问题时，我希望搜索并安装组织已有能力，而不是重复制作。
- 当能力发生更新时，我希望看到差异并安全升级，不覆盖我的本地修改。
- 当能力被分享给全组织时，我希望确认它没有密钥、隐私或危险行为。
- 当项目成员切换 Agent 工具时，我希望同一份项目规则仍可使用。

## 6. 价值主张

### 6.1 对员工

- 一个入口管理多个 Agent 的能力。
- 本地自动发现，减少手工复制和配置。
- 搜索、安装和更新组织验证过的能力。
- 本地修改不会被静默覆盖，所有写文件操作均可预览和恢复。

### 6.2 对团队

- 将个人技巧转化为团队可持续维护的版本化资产。
- 通过团队和项目空间控制共享范围。
- 为能力建立明确的负责人、审核者和更新路径。

### 6.3 对组织

- 统一治理分散在多种 Agent 中的能力。
- 对组织级发布强制审核，并记录完整审计链路。
- 在上传前发现密钥、令牌和疑似隐私内容。
- 通过安装和版本数据识别高价值能力与重复建设。

### 6.4 差异化

Agentdoor 的核心差异不是管理某一种 Agent，而是提供平台中立的能力包、组织空间和治理机制。它同时覆盖本地发现与安装、云端共享、组织权限和版本生命周期。

## 7. 解决方案

### 7.1 产品形态

- macOS 与 Windows 桌面客户端：员工主入口，负责本地扫描、编辑、安全检测、目录绑定、安装和更新。
- Web 管理后台：组织能力市场、成员空间、审核中心、政策、安全报告、审计和分析。
- 云端服务：账号、组织、权限、能力仓库、版本、审核、分发、通知和审计；后端采用 Docker 容器化交付。
- CLI：提供登录、搜索、拉取、发布、安装和同步命令；Linux 首版通过 CLI 使用。

### 7.2 能力包

能力包是 Agentdoor 的统一资产单位，可包含一种或多种内容：

- Skill：可安装到 Agent 的指令、脚本、模板和资源。
- Prompt：可复用的提示词、变量说明和使用示例。
- 项目上下文包：项目规则、约定、术语、架构说明和操作手册，不包含业务源码。

每个能力包具有稳定标识、负责人、说明、标签、兼容 Agent、内容清单、校验摘要和版本历史。已发布版本不可修改；任何变化都生成新版本。

### 7.3 空间

- 个人空间：仅本人可见，用于本地发现、草稿和未发布版本。
- 团队空间：组织内稳定团队共享，可配置是否需要审核。
- 项目空间：组织内跨团队项目共享，可绑定多个本地目录。
- 组织空间：全体组织成员可见，发布必须审核。

能力包继承所在空间的可见范围。MVP 不提供单个能力包的自定义访问控制，避免复杂权限组合。

### 7.4 核心流程

#### 本地发现与沉淀

1. 桌面客户端检测受支持 Agent 和配置目录。
2. 适配器生成本地能力清单与内容摘要。
3. 用户选择需要纳管的内容并导入个人空间。
4. 客户端执行敏感信息、危险脚本和路径安全检测。
5. 通过检测后上传加密能力包并创建草稿版本。

#### 发布与审核

1. 贡献者选择目标团队、项目或组织空间。
2. 系统展示与上一版本的结构化差异和扫描报告。
3. 按空间策略直接发布或进入审核队列。
4. 审核者批准、驳回或要求修改，并留下理由。
5. 通过后生成不可修改的发布版本和审计事件。

#### 安装与更新

1. 用户在桌面端或 Web 能力市场搜索能力。
2. 系统按当前组织、空间成员关系和兼容 Agent 返回结果。
3. 用户预览文件变更、目标路径和风险说明。
4. 客户端通过适配器原子写入，并保存安装清单和可恢复备份。
5. 新版本发布后客户端提示差异；如检测到本地修改，不自动覆盖。

#### 项目上下文同步

1. 项目空间可以关联一台设备上的多个本地目录。
2. 绝对路径只保存在该设备，不上传云端。
3. 用户明确选择要同步的规则和上下文文件。
4. 客户端拒绝超出允许目录、命中忽略规则或疑似源码批量上传的内容。
5. 同一项目空间可把平台中立上下文投影为不同 Agent 的本地格式。

### 7.5 功能范围

#### P0：MVP 必须完成

- 邮箱或手机号注册、验证、登录、找回和设备会话。
- 创建组织、邀请成员、接受邀请、切换组织和退出组织。
- 组织角色与团队、项目空间角色。
- 个人、团队、项目、组织四类空间。
- Skill、Prompt、项目上下文包的创建、导入、编辑和版本管理。
- Codex、Claude Code、Cursor、Gemini CLI 本地适配器。
- 本地发现、安装、卸载、更新、冲突检测和恢复。
- 团队/项目可配置审核；组织级强制审核。
- 安全扫描、风险阻断、风险确认和扫描报告。
- 能力市场的搜索、筛选、详情、版本和兼容性展示。
- 发布、审核、下载、安装、更新和管理操作的审计。
- 基础通知和基础使用统计。
- macOS、Windows 桌面端与 Linux CLI。

#### P1：MVP 稳定后

- 评论、收藏、评分和维护者协作。
- 高级使用分析、重复能力识别和健康度评分。
- 组织策略模板、强制升级和能力下架影响分析。
- OIDC、SAML、LDAP 与 SCIM。
- 语义搜索和组织知识问答。
- 外部协作、跨组织分发和企业私有部署。
- 更多 Agent 适配器与第三方插件 SDK。

### 7.6 安全与隐私

- 全链路 TLS，云端数据库和对象存储加密。
- 密码采用适合密码存储的强哈希算法；刷新令牌轮换并可撤销。
- 上传前进行密钥模式、熵值、隐私词和危险文件检测。
- 服务端对上传包再次校验摘要、文件数量、大小、路径和类型。
- 不允许绝对路径、父目录穿越、符号链接逃逸和未声明可执行文件。
- 组织、空间、能力、版本和审计查询全部执行租户与权限校验。
- 高风险上传默认阻止；可确认风险必须记录确认人和理由。

### 7.7 关键假设

- 研发团队愿意把有效能力分享给团队，只要导入和审核成本足够低。
- Skill、Prompt 和项目上下文可以通过统一清单与文件包表达。
- 首批 Agent 都能通过本地文件目录完成有效的能力安装。
- 组织更需要可见性、版本和安全，而不是首版的复杂社交功能。
- 不上传源码仍能通过规则、文档和能力包提供足够的项目上下文价值。

## 8. 发布计划

### 阶段一：架构与体验骨架

完成能力包规范、账号组织模型、权限策略、适配器接口、核心界面原型和工程基线。建立后端 Docker 镜像、Docker Compose 本地环境和容器发布门禁，并使用一个 Agent 适配器打通纵向闭环。

### 阶段二：MVP 闭环

完成四类空间、四个 Agent 适配器、发布审核、安装更新、安全扫描、能力市场、审计与 Web 管理后台。macOS 和 Windows 桌面端进入内部试点。

### 阶段三：试点稳定

围绕真实安装失败、权限误配、能力重复和审核效率完善可用性、稳定性与数据指标。补齐 Linux CLI 使用路径。

### 阶段四：企业增强

根据试点结果引入企业身份源、高级治理、语义能力和私有部署，不在 MVP 阶段提前建设。

## 验收原则

MVP 的完成标准不是页面齐全，而是一个新用户可以注册并加入组织，发现本地能力，安全地发布到受控空间，由另一位成员审核、安装并在新版本出现后完成更新，且全流程权限正确、可恢复、可审计。
