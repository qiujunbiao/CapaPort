# 项目空间与上下文

项目空间用于把多个本地代码目录关联到同一组织项目知识边界。绑定只保存本地路径、Agent 类型和同步选项；服务器不会收到业务源码。

建议上传：架构原则、命名约定、领域词汇、代码评审清单、部署约束。禁止上传：源码快照、生产数据、私钥、访问令牌、个人敏感信息。路径绑定保存在桌面客户端本地数据库；云端只保存项目空间成员、策略和已明确发布的能力包。

同一项目空间可绑定多个仓库或 monorepo 子目录。每个目录独立开启：规则同步、上下文同步、能力包同步。移除绑定不会删除云端已发布版本，也不会自动删除本地 Agent 文件，删除前会给出明确计划。
